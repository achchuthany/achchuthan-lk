# SPA fallback: serve index.html for client-side routes so deep links and
# page refreshes (e.g. /projects, /academic) resolve instead of 404-ing.
# Recognised by Netlify and Cloudflare Pages. Other hosts need an equivalent
# rewrite (see vercel.json rewrites, or an Apache/Nginx try_files rule).
/*    /index.html   200
